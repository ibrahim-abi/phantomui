{
  "name": "ai-ui",
  "version": "0.1.0",
  "description": "AI-powered UI Testing SDK with MCP Server",
  "private": true,
  "workspaces": [
    "sdk",
    "server",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspace sdk && npm run build --workspace server && npm run build --workspace cli",
    "test": "npm test --workspaces --if-present",
    "demo": "bash demo/run-demo.sh"
  },
  "author": "Muhammad Ibrahim",
  "license": "MIT"
}
